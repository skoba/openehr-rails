<%= @language_code %>:
  layouts:
    application:
  <%= controller_name %>:
    index: &ontology
<% @terms.each do |term| %>      <%= term[:atcode] %>: "<%= term[:item] %>"
<% end %>    new: *ontology
    form: *ontology
    show: *ontology
    edit: *ontology